package LSP_3;

// Liskov Substitution Principle (LSP) Violation Example
// LSP states that objects of a superclass should be replaceable with objects of a subclass without affecting the correctness of the program.
// LSP states that a child class should be able to replace its parent class without causing any issues in the program. 
// This means that the child class should not override any behavior of the parent class that is expected by the client code.

public class LSPViolation {
	
}
